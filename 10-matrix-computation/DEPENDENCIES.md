# MATLAB dependencies

The project-specific functions `heltalsev.m` and `jordanmatris.m`, their entry point, and test matrices are all included. The code otherwise uses standard MATLAB linear-algebra functions such as `eig`, `null`, `compan`, and `blkdiag`.
